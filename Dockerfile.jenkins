FROM jenkins/jenkins:lts-jdk17

USER root

# 安裝 Docker CLI (使用更簡單的方法)
RUN apt-get update && \
    apt-get install -y curl && \
    curl -fsSL https://get.docker.com | sh && \
    usermod -aG docker jenkins && \
    rm -rf /var/lib/apt/lists/*

USER jenkins
